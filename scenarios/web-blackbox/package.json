{
  "name": "@traceforge/scenario-web-blackbox",
  "version": "0.3.0",
  "type": "module",
  "main": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build:runtime": "tsc -p tsconfig.runtime.json && node ../../scripts/verify-scenario-runtime-build.mjs scenarios/web-blackbox",
    "build": "pnpm build:runtime && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@traceforge/orchestration-core": "workspace:*",
    "@traceforge/scenario-sdk": "workspace:*",
    "@traceforge/worker-runtime": "workspace:*"
  }
}
