# Verification criteria

A response difference, error, status code, or suspicious surface is only a candidate signal. Keep separate candidates as separate hypotheses. Validate one hypothesis at a time and record the baseline, controlled change, observed effect, alternative explanations, reproducibility, authorization boundary, and security impact. If any required link is missing, report a limitation instead of a verified finding.
