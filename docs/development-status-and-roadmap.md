# TraceForge 当前开发状态与下一步

更新日期：2026-09-26。本文件只记录当前事实、未完成项和下一优先级。早期批次的覆盖式计划已清除；已完成的实现以源码、测试及相应架构文档为准。

## 产品与架构边界

TraceForge 是 macOS Apple Silicon 优先的桌面 AI 安全智能体。对话是主要入口，任务、工具活动和证据按需展开。Server、通用 Foundation 和 Scenario Process 支持桌面产品；场景声明自己的流程和策略，Core 不包含目标、漏洞或样本特判。运行必须绑定用户授权，模型输出不能扩大授权；停止、撤权、未知执行结果及来源失效继续按持久合同处理。远程执行节点、多用户平台和自动更新不在当前范围。

用户可在桌面编辑场景指导、Skills、知识绑定和 MCP 配置；资源默认值与用户修订分离，新 Run 固定当次生效配置。模型凭据留在宿主安全存储。桌面只提供受保护的宿主桥接，直接打开 Renderer 不显示模拟调查。

## 已实现的当前链路

- 统一对话入口、消息与回复持久化、增量展示、排队编辑、停止与重开回读已接入桌面。任务确认以原消息和设置中的偏好生成实际授权快照；用户明确确认后才派发。
- Scenario Package、Worker、工具发现、受控本机执行、回执、证据与上下文来源由现有合同连接。Web 黑盒是独立 Scenario，不把其页面或验证规则写入 Foundation。
- 原生 Chromium WebContentsView 能在同一桌面窗口展示页面、人工接管、交回、读取同页 DOM 并清理。正常对话入口的一次隔离 `deepseek-flash` 本机网页验收确认：模型发起接管，人工输入后交回，宿主 DOM 回读看到相同文本，两项 Work 完成且无 Work 失败。该 Run 后续仍在规划，用户点击停止，最终是 `run_cancelled`，并非自然 `run_completed`；这项验收不证明安全结论。
- 浏览器面板已按桌面对话工作台的白色阅读面重排：控制权、当前页面、交回与收起操作在同一清晰层级，窄窗口由网页占据主视图；交回成功时立即切换为智能体控制状态，避免旧接管标识触发短暂的连接错误。本机原生验收窗口使用明确标记的合成对话与中性网页，右侧仍是正式 `BrowserViewport` 和真实 `WebContentsView`。[本机实机截图](screenshots/native-browser-acceptance-2026-09-26.jpg)只证明该夹具中的原生界面呈现，不代表正式对话入口或真实目标验收。
- 历史累计门槛的清理已落地：资源/MCP 每 Run 绑定不再受全库 8192 条限制，上下文/Observer 记录不再受全库 8192/4096 条限制，配置历史不再受 2048 版/16 MiB 限制，对话创建不再受 1000 条限制，回复不再受全库 10000 条/32 MiB 池限制，来源项目获取不再受 64 次历史命令限制。旧容量触发器在数据库装配时替换，已有记录不删除；单条输入合同与物理存储准入仍生效。对话列表继续以 1000 条分页读取，流式回复遇存储不足保留已落盘内容并标记 `storage_limit`。
- 本轮扩展了同一原则：移除单会话 2000 条消息、附件与工具回读的全局累计池、上下文资源和记忆历史的固定累计门槛；上下文组装与历史摘要按页消费原始消息，摘要保存覆盖数和逐条来源校验。执行回执、进程记录、恢复/检查点、归档和进程占用的旧默认累计上限迁移为磁盘准入，显式自定义策略仍有效。队列命令、Scenario Artifact/State 默认累计门槛也移除，入库继续检查物理余量。
- Run 工作区可分页列目录、分段读取/追加文件，并以 SHA-256 前置条件原子替换大文件字节区间；脚本写入额度取当前磁盘可用空间，256 KiB 是单次工具传输页而非文件总上限。进程工具输入、执行节点默认写入额度与此同步，不再以 1 GiB 截断；请求和 CPU 时长使用宿主计时器可表示的上限，单进程输出额度为 16 MiB。浏览器会话累计请求/观察/动作和证据库累计条数/字节门槛已移除；单份证据传输上限从 4 MiB 提高到 64 MiB，前端控制帧、采集及读取链同步调整。桌面 stdio MCP 可在明确测试/启用后使用所选站点来源的宿主网络代理，单次请求和进程时长由连接设置指定；凭证留在宿主安全存储，指定环境变量时通过私有管道临时传入已审核的 macOS 沙箱子进程，不进入命令行参数。代理保存按连接和任务归属的网络回执，并在任务绑定撤销后拒绝新连接。
- 已退役旧 Web 应用、旧工具 PoC 和旧 MCP 直启入口；`scripts/verify-retired-code.mjs` 仍在测试与构建门禁中。本轮进一步删除无宿主时的独立演示工作台、模拟审批状态及其无引用样式；缺少桥接时显示明确诊断。旧设计草案、被覆盖的整改计划和临时生成文件已清理，仍由正式宿主使用的共享 CSS 保留；退役代码门禁新增对旧演示入口与状态模块的回归阻断。`build:renderer` 取代了旧 `build:desktop-preview` 命名；原生浏览器验收夹具在启动时重建自己的 JS/CSS/preload，不依赖遗留输出目录。

## 尚未完成与验收边界

1. **任务自然收尾。** 已满足用户目标的 Run 仍可能进入后续无关规划。应从通用任务目标、证据状态和 Scenario 生命周期判断完成，不能为这次本机页面样本添加特殊条件。
2. **本轮能力的原生端到端验收。** 代码与定向回归已覆盖长会话、工作区分页、MCP 网络代理和浏览器证据新额度；本机 macOS Seatbelt 组件测试已验证 MCP 凭证、代理请求与回执。还需在正式桌面入口验证本机大文件、真实浏览器大证据、实际 MCP 服务的配置到调用全链，观察磁盘不足、撤权和进程未知结果。64 MiB 浏览器单份证据和 256 KiB 工作区单次传输页仍是有界传输合同，超过单份浏览器证据额度需要分块采集协议。
3. **发布验收。** 正式桌面发行命令仍受发布门禁拦截。签名、打包、真实目标与平台隔离矩阵应在发布阶段分别核验；现有组件测试或合成截图不算正式发行验收。
4. **真实模型覆盖。** 最近通过的是隔离 `deepseek-flash` 的本机中性网页任务，不代表其他供应商、全部工具组合、长任务或安全发现验证。今后真实模型开发测试只用 DeepSeek，默认不改用户日常模型配置。
5. **资料与配置消费。** 桌面配置能力已有接线；新增或修改配置路径仍需以保存后的真实 Run 消费、版本固定、撤销与恢复验证，不能仅以表单保存视为完成。

## 下一明确优先级

先完成本轮容量与授权能力的原生桌面验收，特别是浏览器大证据、长会话恢复和用户授权的 stdio MCP 联网/凭证调用；再让已满足用户目标的 Scenario Run 自然进入完成状态，并用桌面对话、真实浏览器交接和持久回执验证。每批只记录实际完成的能力和相应证据。

原生验收需要核对授权范围、来源绑定、可回读证据和磁盘余量拒绝；测试通过前不能把新额度描述为真实桌面全链完成。后续自然收尾依赖 Scenario 的通用目标/阶段完成判定与当前 Run 状态投影一致。验收条件：用户授权的中性任务从对话开始，在浏览器交回和所需回执保存后，由场景工作流自己产生 `run_completed`；不创建无关后续 Work，不借停止伪装完成，重开后仍能核对授权、证据与终态。失败、撤权和未知执行结果也不能误写为完成。发布门禁仍按上面的独立缺口处理。

## 验证基线

此前累计门槛整改的相关回归为 **12 文件 / 122 项通过**；Server、Web、Desktop 构建、Scenario 13 个运行模块复现构建、Foundation 307 个生产文件与网络 16 个文件的静态边界检查通过。Web 构建保留既有大分块提示。该批没有运行全仓快速回归，也没有新增真实模型或原生浏览器验收。

本次遗留清理的 Renderer 与桌面入口回归 **38 文件 / 133 项通过**，Web 与 Desktop 构建通过；Desktop 构建包括 13 个可复现 Scenario 运行模块及 1 个内置包。退役代码门禁、Foundation 307 个生产文件边界检查和 `git diff --check` 通过。桌面构建首次在受限环境因 tsx 本机 IPC 管道 `EPERM` 失败，获准在本机重跑后通过。原生夹具资源从源码独立构建通过。真实 Playwright 浏览器打开最终静态构建，页面只显示“桌面连接未就绪”和说明，未出现旧模拟消息/审批；最终刷新无页面资源错误。这项浏览器检查只覆盖无宿主入口，不代表 Electron 主工作台或真实模型整链重新验收。随后以 Node 22 恢复桌面构建留下的数据库原生模块，并在允许本机套接字的环境完整运行 `test:fast`：**313 个测试文件、2715 项测试通过，4 个文件、42 项测试跳过**，耗时 381.51 秒。历史截图、草案和临时产物不能替代上述验收。

本轮容量与授权调整在 Node 22 下完成一次全仓快速回归：**313 个测试文件、2717 项测试通过，4 个文件、42 项测试跳过**，耗时 377.36 秒。随后发现并修复原生 MCP 启动及执行节点写入额度的衔接缺口；修复后执行节点、工作区、MCP 定向回归 **4 文件 / 75 项通过**，Apple Silicon 原生网络与 Seatbelt 回归 **2 文件 / 38 项通过**。本机 Google Chrome 153 的真实浏览器运行测试 **2 项通过**，覆盖页面导航、资源与 DOM 观察、动作、人工接管，以及经审核的 Controller 标准输入输出协议。macOS 隔离助手已从当前 C 源码重建；Foundation、Server、Web、Desktop 构建及退役代码门禁通过，Foundation 边界检查覆盖 307 个生产文件，`git diff --check` 通过。Desktop 构建和原生端口测试在受限环境遇到本机 IPC/监听 `EPERM`，以获准的本机权限重跑后通过。尚未在正式桌面入口完成本轮所有新能力的整链验收，也未进行新的真实模型测试。

本次浏览器面板调整的 Renderer 定向回归 **2 文件 / 6 项通过**，Web 生产构建通过；已有的大分块提示仍在。Electron 本机夹具实际打开原生 `WebContentsView`，以桌面 UI 执行输入、隔离检查、独立滚动和交回，宿主日志确认 `handback`、`nativeInputVisibleToAgent`、`bridgeAbsent` 均为 `true`。夹具的 HTTP Broker 显式使用与生产浏览器相同的 64 MiB 单次响应额度，避免旧 4 MiB 默认值阻断启动。本轮没有重新运行全仓回归或真实模型任务，截图左侧为标明的合成对话。
