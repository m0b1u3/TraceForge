# macOS 原生桌面联合验收记录

## 2026-09-17 补充：配置、队列与 PDF 原生验收

在同一隔离 userData 中安装仓库场景的测试副本；仅测试目录信任临时签名，启动描述仍使用断网权限。测试脚本另提供本地中性 MCP HTTP 服务和两页 PDF，不安装到正式配置，不执行目标任务。原生操作经应用可访问性接口进行，无 renderer 脚本注入。

实际通过：

- Skills：读取包默认来源，创建中性指导并保存为修订 1，停用保存为修订 2，完整退出重开后保持。Markdown 原生选择、替换正文确认、来源文件名及正文保存为修订 3。保存仍只影响新任务。
- MCP：保存修订后明确未测试/未启用；显式测试本地服务才出现 `describe_fixture` 工具目录。新修订改为测试服务的 401 端点后，明确显示认证失败与恢复说明，旧工具目录不再显示。未在本次原生操作中启用或调用 MCP 工具；实际运行消费沿用既有工具网关集成测试，不能混写成原生模型调用 MCP 已验收。
- 队列：真实 Grok 生成期间加入两条消息，暂停接续、修改第一条、提前第二条；当前生成结束后仍有两条待处理，不自动出队。恢复后第二条先进入正在回复状态，最终分别返回 `beta` 和修改后的 `edited-alpha`。完整应用重开后修改后的消息与回复均保留。这里只统计可见逻辑流程，不把它当作上游物理请求精确计数。
- PDF：通过原生选择器导入两页中性文档，实际暴露并修复两个缺陷。改用 PDF.js 配套 legacy renderer/worker 后首页面渲染正常；修正存储类型 `document` 被误判为 `pdf` 后，重开应用成功读取第二页并显示 `Second neutral page`，再翻回第一页显示 `First neutral page`。缩小原生窗口后对话、输入与侧栏可用。
- 工具失败：重开后的旧 `memory_update` 失败记录明确显示“未成功”和“助手文字不代表操作已完成”，保留模型原始回复，不篡改历史。记忆写入失败追加重新检索原文/核对版本的模型反馈，但不保证模型每次遵从，未重跑此前 24 组真实模型可靠性评测。

本批修复：工具返回与成功混淆；Electron 与 PDF.js 现代构建兼容；原生存储 PDF 翻页类型识别；Markdown 文件选择过滤（仅使用 `.md,.txt` 扩展名，继续保留正文读取验证）。新增/扩充持久失败投影、存储 PDF 分页、文件筛选回归。生产权限、Scenario 策略和配置快照纪律未放宽。

两次补充原生实例均正常退出。早先遗留宿主挂起原因仍未证实，正常退出通过不等于根因已修复。测试实例已关闭，Node 数据库依赖已恢复；验收文件留在隔离临时目录，未修改正式配置。

最终回归：9 文件 76 项 + 3 文件 16 项，合计 92 项通过。Skills/MCP 16 项首次被本地监听权限阻断，授权后重跑全部通过；包含指导进入 Worker 上下文、固定版本读取与 MCP 经工具运行时执行。Server、Desktop 和 renderer 构建、脚本语法及差异格式检查通过；保留构建体积提示，没有声称全量或长期耐久测试通过。

下方为 2026-09-16 原始记录；其中配置、队列及 PDF 的“未验收”状态由本节的具体结果覆盖，不能覆盖本节仍明确保留的边界。

环境：macOS Apple Silicon、Electron 37.10.3；生产桌面入口，隔离 userData，沿用 OS 加密保存的 Grok 模型配置。通过原生 UI 操作，不注入 renderer 脚本，不使用合成模型替代本次对话。

## 实际通过

- 同一会话发送中性验收记录，看到真实流式回复、模型公开 reasoning 和工具活动。
- 请求读取历史原文，实际出现 `conversation_read`，回复准确包含测试标记，界面显示已读取 1 段。
- 生成期间发送补充消息，出现待处理状态；前一回复完成后自动接续。另一次生成期间排队后点击停止，部分正文保留，待处理回复也被取消，未继续生成。
- 使用 macOS 原生文件选择器添加仓库内专门创建的无敏感文本附件；保存后点击附件，在同一对话侧栏显示精确内容。
- 正常退出整个应用并从同一隔离目录重开：原会话、完成回复、停止状态、工具记录及附件均保留；界面没有自动续写，再次点击附件可读取相同内容。
- 原生设置页的模型、场景与扩展、MCP 页面能打开。无已安装场景时明确显示依赖条件，未隐式安装或连接服务。

## 不应外推的结论

- 本次没有执行 shell 或黑盒目标任务；历史工具的真实执行不代表所有工具已完成原生验收。
- 隔离目录没有已审核场景，因此 Skills/MCP 已配置后的导入、编辑、生效、诊断与使用关系仍由上一批集成及组件测试覆盖，本次只验证原生空状态。
- 排队与自动接续、停止已实测；队列编辑、排序、暂停的完整原生操作未完成。本次没有追加 PDF 翻页或窄窗原生操作验收。
- 原生重开保持停止状态是可见结果；没有额外统计本次全部模型物理请求，不能据此宣称严格零额外上游调用。上一批独立流式验收另有“不重放”统计。
- 记忆工具曾返回失败后，模型仍声称“已记住”；后续独立原文读取成功。不能把模型的文字确认当作长期记忆写入成功或模型行为可靠性的证明。

## 遗留实例异常

上轮锁屏期间遗留的测试宿主显示页面，但新建消息请求和正常退出不响应。正常退出、TERM 均未结束，经明确批准强制结束指定测试进程。重开同一目录后原草稿与待核对请求保留，点击核对后成功保存并回复，没有通过重新创建消息绕过恢复流程。

挂起根因尚未证实，不记为代码缺陷已修复。之后两个新实例均能正常退出，完整进程重开及附件恢复通过。保留这一异常，后续需用有持久日志的运行方式复现，避免直接把开发终端/进程环境问题泛化为产品规则。

验收结束已关闭隔离实例、恢复 Node 原生数据库依赖。未修改用户正式会话和模型配置，未提交或推送。

恢复后补跑对话路由、桌面联动及配置组件回归：4 文件 / 18 项通过；`git diff --check` 通过。本轮未修改生产代码，沿用上一批完整构建与 53 项专项基线。
