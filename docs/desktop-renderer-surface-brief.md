# 行动谱面工作台 surface brief

## 2026-09-15 模型供应商与推理参数增量

模型设置维持原白底、细边、系统字体及显式保存流程。供应商用原生 optgroup 按国内、国际、Coding 分组，连接差异用相邻提示呈现，不另建供应商卡片墙。“推理与兼容参数”默认折叠，按所选协议显示思考模式、推理强度、温度及加密续接请求选项；继承外层保存期间禁用与未保存保护。切换供应商清除型号及窗口/输出参数，切换型号清除旧型号容量；配置不会自动提交。

Impeccable 检测未发现问题。只读合成模型配置预览检查了 1440×1000 与 390×844，参数区窄屏改单列，未发现横向截断；未使用真实密钥或提交配置。截图 `output/playwright/model-suppliers-desktop.png`、`model-suppliers-narrow.png`。实现为 `model-settings.tsx` 与 `model-request-options.tsx`，不新增全局视觉 token；不代表原生桌面或真实供应商验收。

## 2026-09-15 排队回复与 Markdown 阅读增量

在既有对话面内展示等待接续的回复，并提供明确的“撤回排队”操作。停止正在进行的回复时，同一会话中已排队的后续回复一并取消，避免停止后继续自动接续。排队状态与撤回操作沿用既有轻量文字和按钮，不新增管理面板。

助手正文使用独立的 `message-markdown.tsx` 渲染 Markdown 列表、代码与表格，保留语义结构；正文阅读宽度限制为（75ch）。代码与表格在自身阅读区域处理长内容，窄窗口仍保留对话与输入主线。原始 HTML 不执行；远程图片呈现为需要用户主动打开的链接，不自动加载图像。该显示能力不改变授权、执行或证据判定。

本次是既有白底、墨色文字对话界面的扩展，沿用当前视觉基线，无新增视觉身份。实现来源：`apps/web/renderer/conversation-replies.tsx`、`message-markdown.tsx`、`host-workbench.css`。合成宿主截图 `output/playwright/product-conversation-desktop.png` 与 `output/playwright/product-conversation-narrow.png` 用于桌面及窄窗口核对；评审中的 P2 正文阅读宽度问题已通过（75ch）限制解决，最终 Pass 仅覆盖这次既有界面扩展。

截图中的审批未读异常来自合成 fixture 缺少对应读取端点，不作为真实宿主功能结论。该视觉核对不代表全产品通过、Electron 原生桌面验收或真实模型执行整链完成；运行边界及测试基线以路线图为准。

## 2026-09-15 对话统一入口（覆盖下方旧用途选择与登记后另行启动描述）

宿主消息统一发送给助手，不再显示“消息用途”下拉框。任务请求绑定原始消息，在原消息后复用既有授权卡；普通提问不自动展开调查表单。该卡明确显示“确认后执行本条任务 / 授权并执行本条任务”，用户核对后先登记授权、再沿原 Run 路由派发。独立授权设置不受影响，仍只登记、不启动。后续消息不能改变旧提案的目标；补充任务使用原始用户文字，不代替恢复或扩大权限。

沿用现有白底、细边框和圆角输入面；无新增 token。Playwright 合成宿主截图 `output/playwright/task-entry-desktop.png`、`task-entry-narrow.png` 已核对，无横向溢出，输入器无用途选择器。截图只证明界面状态，不等同原生桌面或真实模型执行整链。

## 2026-09-08 用户确认的桌面方向（覆盖旧宿主表单描述）

批准图：`.impeccable/mocks/authorization-desktop-A-20260908.png`，相邻 JSON 已标记 approved。用户选择中间的“对话内确认卡”，不是右侧授权面板或模态浮层。现有后台式嵌套表单被否定，不再是宿主界面的视觉依据。

| 组成 | 必须保留的视觉关系 | 实现介质 |
| --- | --- | --- |
| 桌面壳 | 矮顶部栏、图标窄导航、标题在顶栏、白色阅读面 | 语义 HTML/CSS，系统原生窗口边框 |
| 对话 | 用户右侧浅灰消息、左侧简洁署名与正文，无大头像和重复状态徽章 | React/CSS |
| 授权 | 正文后单张细边框浅底确认卡，范围为主、动作次之，修改和确认在底部两端 | 通用 Scenario 表单与原生控件 |
| 输入 | 与阅读区对齐的短输入容器、右下圆形发送按钮 | 现有持久会话控制器及 CSS |
| 图标 | 细线导航与 Shield，沿用现有图标库 | Phosphor SVG |

交互阶段必须真实：用户填写后核对，明确同意才登记；授权成功不自动启动任务。图上的 URL 是示例，不能从自然语言猜测并授予更广范围。图中的附件、模型切换与窗口按钮只在已有真实能力支持时呈现，不能做摆设或伪造 macOS 控件。旧独立演示页仍标明演示，宿主新界面样式隔离，不把示例消息接入真实存储。

- Scope: apps/web/renderer，Operate。单用户在本机工作台以对话推进调查，按需核对任务和证据。
- Confirmed: A 并列核查，随后用户明确自己的消息右对齐、智能体左对齐；不沿用旧前端，不重选视觉方向。
- Signature: 对话中的观察引用直接展开侧区来源，行动时间轨与证据分列；窄窗口改为可返回对话的侧区内容切换。
- Constraints: 独立 Workbench 仍为显式演示，输入不冒充已送达模型，模拟审批不执行，来源不冒充 verified Finding。HostWorkbench 的能力与验收状态以路线图为准；任务控件和宿主投影不代表真实模型、生产 Browser 或发行验收完成。
- Approval: .impeccable/mocks/action-score-A.json；最新用户排列修正优先于原始图像。

## 宿主任务与证据增量

2026-09-14 对话记忆增量：在既有助手正文处显示“正在整理上下文 / 正在查阅对话原文 / 正在调整上下文”，保留相邻停止按钮，不另建记忆管理卡片。历史摘要与实际读取原文按需展开；中断文本标明未完成。正式桌面通过固定宿主日志保存草稿与最后会话，重开只恢复视图，不自动发送。已检查原生桌面与紧凑窗口的状态、按钮及输入区，无新增视觉系统。

保留白色阅读面、石板色控件、系统字体和对话主线。宿主输入区为文本与发送按钮两列；在（540px）及以下仍保持可伸缩文本列与（38px）发送列，脚注另起一行，避免继承演示输入区的附件列。导航以浅色选中底和 `aria-current="page"` 同步标识当前内容。

任务面板使用单一“调查任务”标题，表单通过无边框 fieldset 分组，本地待确认记录损坏时整体禁用。授权范围按需展开，启动前的复选确认与启动按钮保持相邻；高级授权登记收起展示。运行状态、任务输出、引用和操作回执分别呈现，不把宿主接受请求表述为执行成功。

证据入口使用单一“任务输出与引用”标题，展示运行记录、任务状态及可展开引用；启动、授权登记和停止操作保留在任务入口。此处是应用状态投影，不承担场景推理或证据结论验证。

## 2026-09-15 对话内思考与工具过程增量

用户要求参考 Codex 的过程阅读体验：在现有对话中随实际记录更新模型公开返回的思考文本、工具调用与命令输出。实现延续白色阅读面、石板色文字、系统字体和细分隔线；助手正文保持阅读主线，过程使用轻量行内展开项，不新增独立控制台或全局视觉 token。

思考与工具过程使用原生 details / summary，可由键盘展开或收起；首次出现的进行中条目默认展开，用户可自行折叠。模型思考只显示供应商公开返回的文本；没有文本时明确提示等待或未提供，不补写内部推理，也不将其视为执行事实或已验证结论。普通助手回复在正文前展示同类思考条目，以及已读取的对话工具参数与返回内容。

工具条目区分请求、派发与结果恢复：请求阶段显示“准备调用”及尚未确认派发的说明，已派发后显示执行状态与持续更新的输出，恢复结果明确说明没有重新执行。命令、调用参数和返回内容均为只读纯文本预览，保留换行并折行；截短时显示有界预览说明。界面不能从模型请求或公开思考推断操作已执行，工具完成仍不代表安全结论已验证。

过程区最大宽度（75ch），标题（13px）、辅助状态（12px）；命令与输出使用浅灰等宽阅读面，字号（12px）、行高（1.65）、圆角（7px），最大高度（320px）。思考正文（14px）、行高（1.75），最大高度（360px）。上述正文区域均可聚焦并用键盘滚动；在（650px）及以下减少内容缩进并允许状态换行，长工具名和长输出折行。最近（8项）过程直接呈现，更早的保留条目按需展开；执行事实记录仍有独立折叠入口。

进展读取失败保留已显示过程并提供重新读取入口；重新连接从已保存事件恢复，不触发工具重跑。组件通过宿主记录的增量读取更新显示，不以模拟打字动画代替真实输出。

实现来源：`apps/web/renderer/execution-trace.tsx`、`run-progress.tsx`、`conversation-replies.tsx`、`host-workbench.css`。本批合成组件截图为 `output/playwright/stream-desktop.png` 与 `output/playwright/stream-narrow.png`，仅用于对应 renderer 状态的桌面与窄窗口视觉核对，不构成真实供应商流式输出、Electron 原生桌面旅程或实际命令执行整链验收。全局 `DESIGN.md` 保持既有视觉基线；运行与验证范围以路线图为准。

## 2026-09-16 大文件附件入口

沿用对话输入框的单一“添加附件”按钮，正式桌面调用原生选择器，PDF / 文本返回本地引用，图片 / 音频沿用小附件输入。读取时显示进行中状态并禁止重复选择；取消不改变既有草稿和附件。失败提示在输入框外完整换行，不能被底部工具栏省略号截断。已选文件仍以可移除的文件名显示，不增加独立上传管理页面。合成桥接验证覆盖选择、取消、失败与 1440×1000 / 390×844 布局；截图 `output/playwright/pdf-reference-desktop.png`、`pdf-reference-narrow.png`。原生文件读取/PDF Worker/系统加密另有 Electron 合成验收，不用 renderer mock 冒充完整原生选择器人工验收。

## 2026-09-16 历史读取回归统一对话

移除“重新核对对话原文”的手动选择器、专属范围及复核完成提示。用户直接在同一输入框提出需求，模型按需查历史，读取后继续任务；不设置另一个只读对话模式。保留现有工具活动、停止、复制和历史详情。成功回读时以轻量文字显示实际读取段数，搜索和记忆更新不计作读过原文。

按 Impeccable 的简化规范删除多余交互和专属样式，保持既有白色对话阅读面。自动组件测试核对无专属复核状态，后端测试核对同轮读取与任务工具共存；本次不声称新增 Electron 原生人工验收。旧复核源码可从前一轮上下文恢复，用户的已保存消息、读取记录及数据库合同不删除。

## 2026-09-16 对话、待处理消息与侧栏预览联动

沿用白底灰阶和左右对话，不增加另一种助手模式。顶部会话状态与发送提示来自宿主回复/Run 投影；等待确认、生成、读取失败明确区分。过程按回合分组，最近 3 回合及每组最近 8 条直接显示，更早的展示条目折叠（仍受既有 100 条投影上限约束），覆盖前述“最近 8 项”的旧展示规则。

对话末尾提供轻量待处理消息折叠区，支持编辑、提前处理、撤回和暂停接续。说明明确：只修改未开始的消息，暂停队列不停止当前任务。未知请求保留本机命令，点击核对不生成新命令；宿主版本冲突不覆盖新状态。

附件、证据引用和工具输出从原位打开侧栏，不切走对话。宽窗侧栏为 320–560px；窄窗上下排列、可滚动，输入和关闭预览仍可操作。标签按会话隔离、最多 8 个，关闭只是关闭视图，不删除文件。文本含 HTML 时仅显示文字；图片只用本地受支持类型；PDF 仅画布预览，按页读取，不加载文档链接或运行脚本。工具输出/差异保持有界快照身份，不伪装完整文件。组件加载或读取错误不应破坏对话。

按 Impeccable 做宽屏/650px 合成宿主界面检查，已修复基础预览样式覆盖窄窗列宽的问题。组件自动化覆盖队列原命令核对、会话切换、迟到预览响应和惰性纯文本输出。此记录不代表真实模型、Electron 原生窗口重开或真实目标任务验收。

## 2026-09-16 Skills / MCP 来源、使用与诊断

沿用现有配置页，不增加新的管理首页。资源正文旁显示包来源或导入文件名、角色/阶段及继承边界；配置历史与任务绑定默认折叠。来源文字不代表可信认证，任务绑定不冒充实际读取或执行证明。旧资源缺少来源时明确说明，不推断成导入文件。

MCP 编辑区分别展示已存凭证、当前修订测试结果和已启用修订；测试失败提供允许公开的恢复说明。凭证输入在操作后清空，其余草稿保留；失败后只读取诊断状态，不自动重试连接。测试与启用仍由明确确认动作触发。只展示最近历史，避免长记录淹没编辑操作。

本轮组件验证包括来源保存、认证失败、绑定展示和无隐式 POST。解锁后已通过原生 UI 验证真实 Grok 回复、历史工具、排队接续、停止、文本附件侧栏与正常退出重开；设置页空状态清楚说明必须先安装审核场景。已配置 Skills/MCP、队列编辑/排序/暂停及 PDF/窄窗原生操作尚未全部实测。详情见 `docs/verification/desktop-native-2026-09-16.md`，不把上述部分验收扩大成所有原生流程通过。

## 2026-09-20 对话任务下的 Browser 控制增量

受控浏览器沿用既有白色阅读面、石板色文字与细灰分隔线，在未结束的对话任务下以原生 details / summary 按需展开，不新增独立浏览器管理页或全局视觉 token。没有会话且没有读取错误时不显示控件；有会话时摘要显示数量，展开后区分“由你控制”“由智能体控制”和“已不可用”，并标明所属工作项。智能体控制时提供“接管浏览器”；人工控制时提供“读取页面元素”和“交回智能体”；会话另有“关闭会话”。操作处理中禁用重复点击，接管说明明确智能体不能操作该页面，操作仍受原任务范围约束。

页面元素视图只用文本呈现元素名称、说明与角色，不嵌入远程 HTML。可编辑元素使用遮蔽输入并关闭自动完成，提供“填入页面”；其他支持的可操作元素提供“点击”。输入草稿在每次命令开始时清空，界面说明输入仅发往当前页面、不保存到对话；操作后清除旧元素视图，需要重新读取。没有可操作元素时显示明确空态。页面内容及其可显示性不构成安全发现、因果机制或验证结论。

读取错误或操作结果未确认时使用 alert；折叠摘要仍显示“需要核对状态”，展开后提供“重新读取状态”。后台读取状态与人工重新读取不会自动重试点击或输入；结果未确认时不宣称操作成功。错误期间接管、读取元素、交回及元素操作禁用，关闭会话仍是显式可用动作。

组件最大宽度（70ch），辅助文字（13px），按钮与输入为白底细边、圆角（8px），键盘焦点有明确轮廓。操作组和元素行允许换行，长名称折行，输入宽度受容器限制；元素区超过（420px）后内部滚动。截图 `output/browser-control-review-wide.png` 将宽、窄容器中的模拟组件并排展示，可用于核对这次布局中的按钮与输入换行；它不是原生整窗截图，也不证明真实接管、页面操作或宿主端到端成功。

实现来源：`apps/web/renderer/browser-session-control.tsx`、`browser-session-control.css` 与 `conversation-execution.tsx`。本次是既有对话 surface 的元素控制扩展，不是像素浏览器、验证码处理或全协议兼容能力，也不是完整 Electron 原生验收。运行合同、后端验证与剩余限制以当前路线图为准。

## 2026-09-20 同窗原生网页增量（补充上方元素控制）

人工接管后，同一 Electron 窗口内可展开任务浏览器，显示智能体所用会话的真实 `WebContentsView` 网页。用户直接在网页中输入、粘贴、滚动和选择文字；显示不通过截图或坐标点击模拟。工具栏保留“由你控制”“交回智能体”和“收起”，地址只展示来源与路径，隐藏查询参数及片段。收起仅隐藏页面，不结束接管；交回先隐藏原生页面，再提交既有恢复操作，结果未确认时明确提示核对。

沿用白底、石板色文字、细边框与系统字体，不新增全局视觉 token。宽窗左侧对话占（46%），右侧网页面板位于顶部栏下方；在（1100px）及以下，网页面板覆盖内容区并保留左侧（72px）导航位置，通过“收起”返回对话。原生网页位于应用自有工具栏、地址与脚注之间，连接中与连接失败有独立提示；“重新连接”只恢复显示，不重复网页操作。应用弹窗出现时隐藏原生网页，避免覆盖确认界面。

原生页使用独立临时会话，启用沙箱及上下文隔离，不装载 preload、不提供 Node 或桌面桥接；弹窗、下载及页面权限请求被拒绝。上述实现边界不等同内核级网络隔离认证，也不证明任意站点、验证码或所有协议兼容。页面仍属于原任务会话，显示及手动操作不扩大任务授权。

本轮真实窗口使用产品面板、合成左侧桌面壳及原生 guest fixture。CUA 原生窗口截图和操作覆盖输入、粘贴、滚动与交回，交回后能读回输入文字，guest 中 Node 与桌面桥接检查均为 undefined。补充键盘验收：“进入网页”将焦点交给网页输入，F6 返回应用按钮，交回后原生页移除。成功连接后不残留“正在显示网页”的辅助技术提示；组件测试确认卸载恢复仍存在的原控件焦点。

`BrowserWindow.capturePage()` 不包含原生子视图，之前生成的 wide/narrow PNG **不能证明网页可见**；当前脚本改为 `output/native-browser-host-only.png`，仅用于宿主布局记录。真实原生视图可见性以本轮 CUA 截图为准。上述验证不构成完整生产任务、真实模型或完整 HostWorkbench 旅程的视觉验收。

实现来源：`apps/web/renderer/browser-viewport.tsx`、`browser-session-control.css` 与 `apps/desktop/src/embedded-browser.ts`。按 Impeccable document 的 surface 范围记录这次既有界面扩展；全局视觉依据仍为根目录 `DESIGN.md`，不重写视觉身份或另建全局 sidecar。运行合同及剩余验收以路线图为准。
