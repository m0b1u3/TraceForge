# TraceForge 桌面设计基线

更新日期：2026-09-24。当前产品界面是 Electron 中的 `HostWorkbench`。桌面对话承担主要工作流，任务、工具活动、授权和证据按需展开。用户消息居右，智能体消息居左；白色阅读面、灰色分隔线和清晰的焦点轮廓保持一致。没有宿主桥接时 Renderer 显示连接错误，不展示模拟调查。

## 当前界面

- 左侧为会话与设置导航，中央为连续对话和输入，右侧材料区按需展开。浏览器活动页面可在同一窗口展示；人工接管改变控制权，不把网页换成截图。
- 任务从统一对话入口提出。确认窗展示原消息、目标和已保存任务偏好，用户明确授权后才开始。偏好设置不是授权，模型文字也不是授权。
- 回复增量显示，思考与工具细节默认收起，仍可查看原始回执。停止回复、停止任务和撤回排队消息保持独立。
- 错误、未知执行结果、失效引用和存储不足明确提示并提供核对入口；旧消息和附件读取保持只读。
- 窄窗口允许材料区替换主视图，返回对话时保留草稿与会话位置。按钮和长文本在窄屏换行，不依赖横向滚动。

## 实现入口

`apps/web/renderer/index.html` 加载 `main.tsx`。桌面桥接进入 `host-workbench.tsx`，模型配置专用模式进入 `model-settings.tsx`。共享样式由 `workbench.css` 提供，宿主布局由 `host-workbench.css` 和各功能样式补充。`apps/desktop/src/preload.cts` 只暴露受保护的宿主桥接。界面与运行时的具体交接、错误状态及最近验收记录见 [工作台界面说明](docs/desktop-renderer-surface-brief.md) 和 [当前计划](docs/development-status-and-roadmap.md)。

## 设计约束

- 不把一条观察或模型输出呈现为已验证的安全结论；引用必须可回查。
- 不把模拟数据、截图或 Renderer 单测描述成真实模型、原生浏览器或完整任务验收。
- 不恢复旧 Web 管理后台、独立模拟审批工作台或其会话存储。
